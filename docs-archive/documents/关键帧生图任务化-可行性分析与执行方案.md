# 关键帧生图接入任务队列 - 可行性分析与执行方案

## 一、现有任务队列运行逻辑分析

### 1.1 任务生命周期

```
PENDING ──► PROCESSING ──► COMPLETED/FAILED
   │            │
   │            ▼
   │      持久化到 jobs_queue.json
   │            │
   │            ▼
   │      调用 AI Provider
   │            │
   │            ▼
   │      下载/保存资源
   │            │
   │            ▼
   │      更新 Asset/Script
   │            │
   └────────────┘
         notify() 通知 UI
```

### 1.2 核心机制验证

| 机制       | 实现位置                                  | 验证结果                |
| ---------- | ----------------------------------------- | ----------------------- |
| 任务持久化 | `storage.ts:saveJob()`                    | ✅ 写入 jobs_queue.json |
| 并发控制   | `queue.ts:processQueue()`                 | ✅ maxConcurrentJobs    |
| 任务认领   | `storage.ts:claimJob()`                   | ✅ 防止重复处理         |
| 状态通知   | `queue.ts:notify()`                       | ✅ 订阅模式             |
| 任务恢复   | `queue.ts:processQueue()` stale jobs 处理 | ✅ 重启后恢复           |

### 1.3 现有任务类型处理逻辑

```typescript
// queue.ts executeJob() 中的任务分发
if (currentJob.type === 'generate_image') {
  // 1. 调用 aiService.generateImage()
  // 2. 下载图片
  // 3. 保存到 generated/{typeDir}/{assetName}/
  // 4. 调用 updateAssetWithResult() 更新 Asset
} else if (currentJob.type === 'generate_video') {
  // 1. 调用 aiService.generateVideo()
  // 2. 下载视频
  // 3. 保存到 generated/{typeDir}/{assetName}/
  // 4. 调用 updateAssetWithResult() 更新 Asset
}
```

**关键发现**:

- 所有任务最终都通过 `updateAssetWithResult()` 更新资产
- 关键帧数据存储在 `Script.parseState.shots[].keyframes[]` 中，不是 Asset
- 需要新的更新机制来处理 Script 中的关键帧数据

---

## 二、方案可行性分析

### 2.1 技术可行性 ✅

| 检查项       | 结论    | 说明                                          |
| ------------ | ------- | --------------------------------------------- |
| Job 类型扩展 | ✅ 可行 | `type` 字段是字符串联合类型，可扩展           |
| 任务参数传递 | ✅ 可行 | `params` 是 `any` 类型，可包含关键帧特有参数  |
| 参考图传递   | ✅ 可行 | 现有 `referenceImages` 字段已支持 base64 数组 |
| 结果存储     | ✅ 可行 | `result` 是 `any` 类型，可存储 GeneratedImage |
| Script 更新  | ✅ 可行 | `storage.ts` 已有 `saveScript()` 方法         |

### 2.2 与现有架构兼容性 ✅

```
┌─────────────────────────────────────────────────────────────┐
│                     现有架构兼容点                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. Job 创建模式                                            │
│     - 现有: aiService.createGenerationJobs()               │
│     - 新增: aiService.createKeyframeGenerationJob()        │
│     - 兼容: 同样的 Job 结构，只是 type 不同                 │
│                                                             │
│  2. 任务提交方式                                            │
│     - 现有: jobQueue.addJob(job)                           │
│     - 新增: 相同方式                                        │
│     - 兼容: 无需改变接口                                    │
│                                                             │
│  3. 任务处理流程                                            │
│     - 现有: executeJob() 中按 type 分发                     │
│     - 新增: 添加 generate_keyframe_image 分支               │
│     - 兼容: 符合现有模式                                    │
│                                                             │
│  4. UI 更新机制                                             │
│     - 现有: subscribe() + notify()                         │
│     - 新增: 相同机制                                        │
│     - 兼容: JobMonitor 自动支持新类型                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 需要解决的关键问题

#### 问题 1: Script 更新与 Asset 更新的差异

**现有逻辑** (Asset 更新):

```typescript
// queue.ts updateAssetWithResult()
await storageService.updateAsset(job.params.assetId, projectId, async asset => {
  // 更新 asset.generatedImages
  return asset;
});
```

**新增逻辑** (Script 关键帧更新):

```typescript
// 需要新增方法
await storageService.updateKeyframeInScript(
  scriptId,
  shotId,
  keyframeId,
  projectId,
  async keyframe => {
    // 更新 keyframe.generatedImages
    return keyframe;
  }
);
```

**解决方案**: 在 `queue.ts` 中直接调用 `storageService.saveScript()`，不需要新增 storage 方法。

#### 问题 2: 参考图 base64 体积

**分析**: 关键帧生图需要传递角色图和场景图作为参考，base64 可能较大。

**现有方案验证**:

- 现有 `generate_image` 任务已支持 `referenceImages: string[]`
- 代码中直接传递 base64 到 AI Provider
- 没有大小限制处理

**结论**: 沿用现有方案，不做额外处理。

#### 问题 3: 任务恢复 (Stale Job)

**分析**: 应用重启后，需要恢复进行中的关键帧生图任务。

**现有逻辑** (generate_image):

```typescript
// queue.ts processQueue() stale jobs 处理
if (job.type === 'generate_image') {
  // 检查 asset.generatedImages 中是否有新图
  // 如果有，标记为 COMPLETED
  // 如果没有，重置为 PENDING
}
```

**新增逻辑** (generate_keyframe_image):

```typescript
// 需要添加类似逻辑
if (job.type === 'generate_keyframe_image') {
  // 检查 script.parseState.shots[].keyframes[].generatedImages
  // 如果有新图，标记为 COMPLETED
  // 如果没有，重置为 PENDING
}
```

---

## 三、修正后的执行方案

### 3.1 修改范围（5个文件）

| 序号 | 文件                    | 修改类型  | 工作量 |
| ---- | ----------------------- | --------- | ------ |
| 1    | `types.ts`              | 修改      | 小     |
| 2    | `services/aiService.ts` | 新增      | 中     |
| 3    | `services/queue.ts`     | 修改+新增 | 大     |
| 4    | `locales.ts`            | 修改      | 小     |
| 5    | `views/ShotManager.tsx` | 修改      | 中     |

### 3.2 详细修改内容

#### 文件 1: types.ts

**修改点**: 扩展 Job.type 联合类型

```typescript
// 第122行
export interface Job {
  id: string;
  type: 'generate_image' | 'generate_video' | 'generate_keyframe_image'; // 新增
  status: JobStatus;
  projectId: string;
  params: any;
  result?: any;
  error?: string;
  createdAt: number;
  updatedAt: number;
}
```

**验证**: `params` 和 `result` 已经是 `any` 类型，无需修改。

---

#### 文件 2: services/aiService.ts

**新增接口** (第38行后):

```typescript
export interface KeyframeGenerationJobParams {
  projectId: string;
  scriptId: string;
  shotId: string;
  keyframeId: string;
  prompt: string;
  userPrompt?: string;
  assetName: string;
  modelConfigId: string;
  referenceImages?: string[];
  resolution?: string;
  aspectRatio?: string;
}
```

**新增方法** (第159行后):

```typescript
/**
 * 创建关键帧生图任务
 */
createKeyframeGenerationJob(params: KeyframeGenerationJobParams): Job {
    const job: Job = {
        id: typeof crypto.randomUUID === 'function'
            ? crypto.randomUUID()
            : Math.random().toString(36).substring(2) + Date.now().toString(36),
        projectId: params.projectId,
        type: 'generate_keyframe_image',
        status: JobStatus.PENDING,
        createdAt: Date.now(),
        updatedAt: Date.now(),
        params: {
            scriptId: params.scriptId,
            shotId: params.shotId,
            keyframeId: params.keyframeId,
            prompt: params.prompt,
            userPrompt: params.userPrompt,
            model: params.modelConfigId,
            modelConfigId: params.modelConfigId,
            assetName: params.assetName,
            referenceImages: params.referenceImages,
            resolution: params.resolution,
            aspectRatio: params.aspectRatio,
            generateCount: 1
        }
    };

    return job;
}
```

---

#### 文件 3: services/queue.ts

**修改 executeJob 方法** (第462行后):

```typescript
else if (currentJob.type === 'generate_keyframe_image') {
    await this.executeKeyframeGenerationJob(currentJob);
}
```

**新增方法 1** - 关键帧生图执行:

```typescript
private async executeKeyframeGenerationJob(job: Job) {
    console.log(`[Queue] 开始关键帧生图任务 ${job.id}`);

    const {
        scriptId, shotId, keyframeId,
        prompt, modelConfigId, referenceImages, resolution, aspectRatio
    } = job.params;

    // 1. 调用AI生图
    const result = await aiService.generateImage(
        prompt,
        modelConfigId,
        referenceImages || [],
        aspectRatio,
        resolution,
        1
    );

    if (!result.success || !result.data) {
        throw new Error(result.error || "关键帧生图失败");
    }

    // 2. 下载并保存图片
    const imageData = Array.isArray(result.data) ? result.data[0] : result.data;
    const imageUrl = imageData.url || imageData.path;

    let blob: Blob;
    if (imageUrl.startsWith('http://') || imageUrl.startsWith('https://')) {
        try {
            const proxyUrl = `/api/proxy?url=${encodeURIComponent(imageUrl)}`;
            const proxyRes = await fetch(proxyUrl);
            if (!proxyRes.ok) throw new Error('Proxy fetch failed');
            blob = await proxyRes.blob();
        } catch {
            const directRes = await fetch(imageUrl);
            if (!directRes.ok) throw new Error('Direct fetch failed');
            blob = await directRes.blob();
        }
    } else {
        throw new Error("Invalid image URL");
    }

    // 3. 保存到本地
    const timestamp = Date.now();
    const random = Math.random().toString(36).substring(2, 8);
    const ext = blob.type === 'image/webp' ? 'webp' : blob.type === 'image/png' ? 'png' : 'jpg';
    const filename = `${timestamp}_${random}.${ext}`;
    const path = `generated/keyframes/${filename}`;

    await storageService.saveBinaryFile(path, blob);

    // 4. 构建 GeneratedImage
    const generatedImage: GeneratedImage = {
        id: crypto.randomUUID(),
        path: path,
        prompt: prompt,
        userPrompt: job.params.userPrompt,
        modelConfigId: modelConfigId,
        modelId: imageData.modelId || modelConfigId,
        referenceImages: referenceImages || [],
        createdAt: Date.now(),
        width: imageData.width,
        height: imageData.height
    };

    // 5. 更新 Script
    await this.updateKeyframeInScript(job, generatedImage);

    // 6. 设置结果
    job.result = { path, generatedImage };
    job.status = JobStatus.COMPLETED;
}
```

**新增方法 2** - 更新 Script 中的关键帧:

```typescript
private async updateKeyframeInScript(job: Job, generatedImage: GeneratedImage) {
    const { scriptId, shotId, keyframeId } = job.params;

    const script = await storageService.getScript(scriptId, job.projectId);
    if (!script || !script.parseState?.shots) {
        throw new Error(`Script not found: ${scriptId}`);
    }

    const shot = script.parseState.shots.find(s => s.id === shotId);
    if (!shot || !shot.keyframes) {
        throw new Error(`Shot or keyframes not found: ${shotId}`);
    }

    const keyframe = shot.keyframes.find(k => k.id === keyframeId);
    if (!keyframe) {
        throw new Error(`Keyframe not found: ${keyframeId}`);
    }

    // 更新关键帧
    if (!keyframe.generatedImages) {
        keyframe.generatedImages = [];
    }
    keyframe.generatedImages.push(generatedImage);
    keyframe.currentImageId = generatedImage.id;
    keyframe.generatedImage = generatedImage;
    keyframe.status = 'completed';

    await storageService.saveScript(script);
}
```

**修改 stale jobs 处理** (第243行后):

```typescript
else if (job.type === 'generate_keyframe_image') {
    const { scriptId, shotId, keyframeId } = job.params;

    if (scriptId && shotId && keyframeId) {
        try {
            const script = await storageService.getScript(scriptId, job.projectId);
            const shot = script?.parseState?.shots?.find(s => s.id === shotId);
            const keyframe = shot?.keyframes?.find(k => k.id === keyframeId);

            if (keyframe?.generatedImages && keyframe.generatedImages.length > 0) {
                // 检查是否有任务创建后生成的图片
                const newImages = keyframe.generatedImages.filter(
                    img => img.createdAt >= job.createdAt
                );

                if (newImages.length > 0) {
                    const latest = newImages[newImages.length - 1];
                    job.result = { path: latest.path, generatedImage: latest };
                    job.status = JobStatus.COMPLETED;
                    job.updatedAt = Date.now();
                    await storageService.saveJob(job);
                    this.notify(job);
                    continue;
                }
            }
        } catch {}
    }

    // 未找到结果，重置为 PENDING
    job.status = JobStatus.PENDING;
    job.updatedAt = Date.now();
    await storageService.saveJob(job);
    this.notify(job);
}
```

---

#### 文件 4: locales.ts

**修改点**: 添加多语言文本 (第312行和第759行):

```typescript
// en
jobTypes: {
  generate_image: "Generate Image",
  generate_video: "Generate Video",
  generate_keyframe_image: "Generate Keyframe"  // 新增
}

// zh
jobTypes: {
  generate_image: "生成图片",
  generate_video: "生成视频",
  generate_keyframe_image: "生成关键帧"  // 新增
}
```

---

#### 文件 5: views/ShotManager.tsx

**修改点 1**: 导入 jobQueue (第10行后):

```typescript
import { jobQueue } from '../services/queue';
```

**修改点 2**: 修改 handleGenerateImage 方法 (第466行):

```typescript
const handleGenerateImage = async (keyframeIndex: number) => {
  if (!projectId || !selectedShot || !currentScript) return;

  if (availableImageModels.length === 0) {
    showToast('请先在设置中配置生图模型', 'error');
    return;
  }

  if (!selectedImageModel) {
    showToast('请先选择生图模型', 'error');
    return;
  }

  const kf = selectedShot.keyframes?.[keyframeIndex];
  if (!kf) return;

  // 获取参考图
  const assets = await storageService.getAssets(projectId);
  const characterAsset = kf.references?.character
    ? (assets.find(
        a => a.type === AssetType.CHARACTER && a.name === kf.references.character.name
      ) as CharacterAsset)
    : undefined;
  const sceneAsset = kf.references?.scene
    ? assets.find(a => a.type === AssetType.SCENE && a.name === kf.references.scene.name)
    : undefined;

  // 准备参考图 base64
  const referenceImages: string[] = [];

  const imageToBase64 = async (filePath: string): Promise<string> => {
    const file = await storageService.getFile(filePath);
    if (!file) throw new Error(`文件不存在: ${filePath}`);
    const arrayBuffer = await file.arrayBuffer();
    const uint8Array = new Uint8Array(arrayBuffer);
    let binary = '';
    for (let i = 0; i < uint8Array.length; i++) {
      binary += String.fromCharCode(uint8Array[i]);
    }
    const base64 = btoa(binary);
    const ext = filePath.split('.').pop()?.toLowerCase() || 'png';
    return `data:image/${ext};base64,${base64}`;
  };

  if (characterAsset?.currentImageId) {
    const charImage = characterAsset.generatedImages?.find(
      img => img.id === characterAsset.currentImageId
    );
    if (charImage?.path) {
      try {
        const base64 = await imageToBase64(charImage.path);
        referenceImages.push(base64);
      } catch (e) {
        console.error('读取角色图失败:', e);
      }
    }
  }

  if (sceneAsset?.filePath) {
    try {
      const base64 = await imageToBase64(sceneAsset.filePath);
      referenceImages.push(base64);
    } catch (e) {
      console.error('读取场景图失败:', e);
    }
  }

  // 创建并提交任务
  const job = aiService.createKeyframeGenerationJob({
    projectId,
    scriptId: currentScript.id,
    shotId: selectedShot.id,
    keyframeId: kf.id,
    prompt: kf.prompt,
    userPrompt: kf.description,
    assetName: `${selectedShot.sceneName}-镜头${selectedShot.sequence}-关键帧${kf.sequence}`,
    modelConfigId: selectedImageModel,
    referenceImages,
    resolution: calculateSize,
    aspectRatio: selectedAspectRatio,
  });

  await jobQueue.addJob(job);
  showToast('关键帧生图任务已添加到队列', 'success');
};
```

**修改点 3**: 移除本地 loading 状态 (第174行):

```typescript
// 移除
// const [generatingKeyframeId, setGeneratingKeyframeId] = useState<string | null>(null);
```

**修改点 4**: 移除 isLoading 判断 (第991行):

```typescript
// 原代码
isLoading={generatingKeyframeId === selectedShot.keyframes[selectedKeyframeIndex].id}

// 新代码 - 使用 keyframe.status 判断
isLoading={selectedShot.keyframes[selectedKeyframeIndex].status === 'generating'}
```

---

### 3.3 预期效果验证

| 功能            | 验证方式             | 预期结果                                    |
| --------------- | -------------------- | ------------------------------------------- |
| 任务进入队列    | 检查 jobs_queue.json | 包含 type: 'generate_keyframe_image' 的任务 |
| JobMonitor 显示 | 观察浮窗             | 显示"生成关键帧"任务及进度                  |
| 并发控制        | 同时提交多个任务     | 最多 maxConcurrentJobs 个同时处理           |
| 任务恢复        | 重启应用             | 未完成的任务自动恢复为 PENDING              |
| Script 更新     | 检查 Script 数据     | keyframe.generatedImages 包含新图片         |

---

## 四、风险评估

| 风险               | 概率 | 影响 | 缓解措施                            |
| ------------------ | ---- | ---- | ----------------------------------- |
| Script 数据竞争    | 低   | 中   | 使用 storage.ts 的 lock 机制        |
| 参考图 base64 过大 | 中   | 低   | 与现有生图任务一致，无额外风险      |
| 任务恢复逻辑复杂   | 低   | 中   | 充分测试 stale job 处理             |
| UI 状态不同步      | 中   | 低   | 依赖 subscribe 机制，与现有任务一致 |

---

## 五、测试建议

1. **单元测试**: 测试 `createKeyframeGenerationJob()` 方法
2. **集成测试**: 测试完整的关键帧生图流程
3. **并发测试**: 同时提交多个关键帧生图任务
4. **恢复测试**: 模拟应用重启，验证任务恢复
5. **边界测试**: 无参考图、大参考图、网络失败等场景

---

_分析完成时间: 2026-02-14_
_结论: 方案可行，与现有架构完全兼容_
