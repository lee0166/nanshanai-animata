# 小说解析流程优化 v2.0 - 验收检查清单

---

## 说明

本检查清单按实施阶段组织，每个模块完成后可逐项检查验收。

---

## Phase 1: 基础架构（立即可做）

### Module 1: JSON修复模块

**实施前检查**:

- [ ] 确认 `services/` 目录存在
- [ ] 确认可以新增文件

**实施后检查**:

- [ ] 文件 `services/JSONRepair.ts` 已创建
- [ ] 实现了 `repair` 静态方法
- [ ] 实现了 `validate` 静态方法
- [ ] 支持引号不匹配修复
- [ ] 支持括号不匹配修复
- [ ] 支持多余内容截断
- [ ] 集成到 `scriptParser.ts` 的解析流程
- [ ] 单元测试通过率 100%
- [ ] 修复成功率 > 95%

**回退检查**:

- [ ] 可以通过配置关闭JSON修复
- [ ] 关闭后使用原有解析逻辑

---

### Module 2: 语义分块模块

**实施前检查**:

- [ ] 已阅读现有 `chunkText` 方法
- [ ] 确认 `types.ts` 可修改

**实施后检查**:

- [ ] 新增 `SemanticChunk` 接口定义
- [ ] 实现 `semanticChunk` 方法
- [ ] 支持章节边界识别(【第一章】等)
- [ ] 支持段落边界识别
- [ ] 支持句子边界识别
- [ ] 提取分块元数据(角色、场景提示、重要性)
- [ ] 保留前200字上下文
- [ ] 保留原有 `chunkText` 作为fallback
- [ ] 单元测试通过率 100%
- [ ] 章节边界识别准确率 > 90%
- [ ] 10万字分块时间 < 5秒

**回退检查**:

- [ ] 可以通过配置切换回 `chunkText`
- [ ] 切换后功能正常

---

### Module 3: 模型路由

**实施前检查**:

- [ ] 确认 `LLMProvider` 接口
- [ ] 确认可用的模型列表

**实施后检查**:

- [ ] 文件 `services/ModelRouter.ts` 已创建
- [ ] 定义 `TaskType` 类型
- [ ] 实现模型配置映射
- [ ] 支持 metadata → gpt-4o-mini/deepseek
- [ ] 支持 character → gpt-4o/claude
- [ ] 支持 scene → gpt-4o/claude
- [ ] 支持 shot → gpt-4o/claude
- [ ] 实现降级策略
- [ ] 实现限流控制
- [ ] 集成到 `scriptParser.ts`
- [ ] 保留 `CONFIG.defaultModel` 作为fallback
- [ ] 单元测试通过率 100%
- [ ] 降级策略测试通过率 100%

**回退检查**:

- [ ] 可以通过配置禁用模型路由
- [ ] 禁用后使用 `CONFIG.defaultModel`

**效果验证**:

- [ ] 元数据提取使用低成本模型
- [ ] 成本降低 40%+

---

### Module 4: 子任务级状态管理

**实施前检查**:

- [ ] 确认 `ScriptParseState` 接口
- [ ] 确认 `storageService` 接口

**实施后检查**:

- [ ] 扩展 `ScriptParseState` 接口(新增subTasks字段)
- [ ] 新增 `SubTaskState` 类型
- [ ] 实现子任务状态追踪
- [ ] 实现 `resume` 断点续传方法
- [ ] 实现失败重试机制(最多3次)
- [ ] 实现指数退避
- [ ] 状态持久化到 storage
- [ ] 单元测试通过率 100%
- [ ] 断点续传功能正常
- [ ] 已完成的子任务不重复执行

**回退检查**:

- [ ] 可以忽略 `subTasks` 字段
- [ ] 使用原有阶段级逻辑

---

## Phase 2: 效率优化（可选）

### Module 5: 多级缓存系统

**实施前检查**:

- [ ] 确认可以使用 IndexedDB
- [ ] 确认缓存大小限制

**实施后检查**:

- [ ] 文件 `services/MultiLevelCache.ts` 已创建
- [ ] 实现 L1 全文级缓存(内存)
- [ ] 实现 L2 实体级缓存(IndexedDB)
- [ ] 实现 L3 资产映射缓存(IndexedDB)
- [ ] 实现缓存TTL机制
- [ ] 实现增量更新检测
- [ ] 集成到 `scriptParser.ts`
- [ ] 单元测试通过率 100%
- [ ] 缓存命中率 > 60%
- [ ] 缓存读写性能 < 100ms

**回退检查**:

- [ ] 可以通过配置禁用缓存
- [ ] 缓存miss时走正常流程

---

### Module 6: RAG检索系统

**前置确认**:

- [ ] 已安装 `chromadb` 依赖
- [ ] 确认可以接受打包体积增加

**实施后检查**:

- [ ] 文件 `services/RAGRetriever.ts` 已创建
- [ ] 集成 ChromaDB
- [ ] 实现 Embedding 生成
- [ ] 实现 `retrieveForCharacter` 方法
- [ ] 实现 `retrieveForScene` 方法
- [ ] 实现相似度排序
- [ ] 集成到 `scriptParser.ts`
- [ ] 单元测试通过率 100%
- [ ] 检索响应时间 < 500ms
- [ ] 角色检索准确率 > 80%

**回退检查**:

- [ ] 可以通过配置关闭RAG
- [ ] 关闭后使用全文解析

---

## Phase 3: 业务优化（需确认需求）

### 需求确认清单

**短剧规则引擎**:

- [ ] 确认系统用于短剧制作
- [ ] 确认需要黄金3秒规则
- [ ] 确认需要单集时长限制
- [ ] 确认规则引擎配置参数

**人工确认UI**:

- [ ] 确认需要导演确认流程
- [ ] 确认UI设计需求
- [ ] 确认锁定机制要求

**资产相似度匹配**:

- [ ] 确认需要资产推荐
- [ ] 确认相似度阈值

---

## 最终验收标准

### Phase 1 完成后

**性能指标**:

- [ ] 解析成功率 > 99%
- [ ] 解析时间减少 20%+
- [ ] 解析成本降低 40%+

**功能指标**:

- [ ] JSON修复成功率 > 95%
- [ ] 语义分块准确率 > 90%
- [ ] 模型路由正确选择
- [ ] 断点续传功能正常

**兼容性**:

- [ ] 所有现有功能正常
- [ ] 旧数据格式兼容
- [ ] 配置文件兼容

### Phase 2 完成后

**性能指标**:

- [ ] 缓存命中率 > 60%
- [ ] 重复解析成本为零
- [ ] RAG检索响应 < 500ms

### Phase 3 完成后

**业务指标**:

- [ ] 短剧规则符合业务需求
- [ ] 人工确认流程顺畅
- [ ] 资产推荐准确率 > 70%

---

## 回退验证

### Phase 1 回退

- [ ] JSON修复可关闭
- [ ] 语义分块可切换回原有逻辑
- [ ] 模型路由可禁用
- [ ] 状态管理可使用原有逻辑

### Phase 2 回退

- [ ] 缓存可禁用
- [ ] RAG可关闭

### Phase 3 回退

- [ ] 短剧规则引擎可关闭
- [ ] 人工确认可跳过

---

**文档版本**: v2.0  
**最后更新**: 2026-02-21
