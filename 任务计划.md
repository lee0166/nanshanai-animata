## 新任务窗口执行说明

### 任务背景

在之前的会话中，我们成功实施了分镜生成优化并通过了测试（92 个分镜，80/100 质量评分），但由于 Git 分支管理问题，优化代码未能提交到 master 分支。

现在需要**重新执行分镜生成优化**，测试通过后正式提交到 master 并推送到 GitHub。

---

### 优化目标

**解决核心问题**：

- 分镜生成为 0 个（超时导致）
- 超时失败率 100%
- 质量评分 0/100

**预期效果**：

- 分镜生成成功率：0% → >95%
- 分镜数量：0 → 90-110 个
- 质量评分：0 → >75/100
- 超时失败率：100% → <5%

---

### Phase 1: 紧急修复（必须执行）

#### 任务 1.1: 修改 DynamicTimeoutCalculator.ts

**文件路径**: `services/parsing/DynamicTimeoutCalculator.ts`

**修改内容**:

```typescript
// 在 calculateDynamicSafetyFactor 方法中添加分镜生成特殊处理
private calculateDynamicSafetyFactor(avgTime: number, taskType: string): number {
  // 分镜生成任务使用更高的安全系数
  if (taskType === 'shots') {
    if (avgTime < 60000) return 3.0;   // <1 分钟：3.0 倍
    if (avgTime < 120000) return 2.5;  // 1-2 分钟：2.5 倍
    return 2.3;                         // >2 分钟：2.3 倍
  }

  // 其他任务使用原有逻辑
  if (avgTime < 30000) return 1.5;
  if (avgTime < 60000) return 2.0;
  if (avgTime < 120000) return 2.3;
  return 2.5;
}
```

**验证点**:

- ✅ 分镜生成安全系数：3.0/2.5/2.3
- ✅ 其他任务保持原有逻辑
- ✅ 类型检查通过

---

#### 任务 1.2: 修改 scriptParser.ts - 添加动态批次调整

**文件路径**: `services/scriptParser.ts`

**修改位置**: `generateAllShotsWithContext` 方法

**修改内容**:

1. **在方法开头添加批次配置**（约 4720 行位置）:

```typescript
const targetShots = shotGen.targetShots;

// Phase 1.2: 动态批次大小调整
interface BatchSizeConfig {
  initial: number;
  min: number;
  max: number;
  adjustThreshold: {
    decrease: number; // 响应时间超过此值减小批次
    increase: number; // 响应时间低于此值增大批次
  };
}

const BATCH_SIZE_CONFIG: BatchSizeConfig = {
  initial: 15, // 从 20 减少到 15
  min: 8,
  max: 25,
  adjustThreshold: {
    decrease: 90000, // 90 秒
    increase: 60000, // 60 秒
  },
};

const batchSize = this.parserConfig.shotBatchSize || BATCH_SIZE_CONFIG.initial;
const maxBatches = this.parserConfig.maxShotBatches || 8;
const minShotsThreshold = Math.floor(targetShots * 0.6);

let allShots: Shot[] = [];
let currentBatch = 0;
let consecutiveFailures = 0;
const maxConsecutiveFailures = 2;

// 动态批次大小状态
let currentBatchSize = batchSize;
let lastResponseTime = 0;
```

2. **修改批次循环逻辑**（约 4756 行位置）:

```typescript
while (allShots.length < targetShots && currentBatch < maxBatches && consecutiveFailures < maxConsecutiveFailures) {
  currentBatch++;
  const remainingShots = targetShots - allShots.length;

  // 动态调整批次大小
  const actualBatchSize = Math.min(
    currentBatchSize,
    remainingShots,
    BATCH_SIZE_CONFIG.max
  );

  console.log(
    `[ScriptParser] ========== Batch ${currentBatch}: Generating ${actualBatchSize} shots (${allShots.length + 1}-${allShots.length + actualBatchSize} of ${targetShots}) ==========`
  );

  // ... 现有上下文处理代码 ...

  try {
    // 记录批次开始时间
    const batchStartTime = Date.now();
    const batchShots = await this.generateSingleBatch(batchPrompt, allShots.length);
    lastResponseTime = Date.now() - batchStartTime;

    if (batchShots.length === 0) {
      consecutiveFailures++;
      console.warn(`[ScriptParser] Batch ${currentBatch} returned 0 shots, consecutive failures: ${consecutiveFailures}`);
      continue;
    }

    consecutiveFailures = 0;
    allShots = [...allShots, ...batchShots];

    console.log(
      `[ScriptParser] Batch ${currentBatch} complete: ${batchShots.length} shots generated, total: ${allShots.length}/${targetShots}, response time: ${lastResponseTime}ms`
    );

    // 根据响应时间调整下一批次大小
    currentBatchSize = this.adjustBatchSize(
      currentBatchSize,
      lastResponseTime,
      BATCH_SIZE_CONFIG
    );
```

3. **添加批次大小调整方法**（在 generateSingleBatch 方法之前）:

```typescript
/**
 * 调整批次大小（Phase 1.2 动态批次调整）
 */
private adjustBatchSize(
  currentSize: number,
  responseTime: number,
  config: {
    min: number;
    max: number;
    adjustThreshold: {
      decrease: number;
      increase: number;
    };
  }
): number {
  if (responseTime > config.adjustThreshold.decrease) {
    // 响应慢：减小批次
    const newSize = Math.max(config.min, currentSize - 3);
    console.log(
      `[ScriptParser] Adjusting batch size: ${currentSize} → ${newSize} ` +
      `(response time: ${responseTime}ms > ${config.adjustThreshold.decrease}ms)`
    );
    return newSize;
  }

  if (responseTime < config.adjustThreshold.increase) {
    // 响应快：增大批次
    const newSize = Math.min(config.max, currentSize + 2);
    console.log(
      `[ScriptParser] Adjusting batch size: ${currentSize} → ${newSize} ` +
      `(response time: ${responseTime}ms < ${config.adjustThreshold.increase}ms)`
    );
    return newSize;
  }

  // 响应正常：保持不变
  return currentSize;
}
```

**验证点**:

- ✅ 初始批次：15 个
- ✅ 动态范围：8-25 个
- ✅ 响应>90 秒：减小 3 个
- ✅ 响应<60 秒：增加 2 个
- ✅ 类型检查通过

---

### Phase 2: 中期优化（建议执行）

#### 任务 2.1: 优化渐进式上下文配置

**文件路径**: `services/scriptParser.ts`

**修改位置**: `generateAllShotsWithContext` 方法中的渐进式上下文部分

**修改内容**:

```typescript
// ISSUE-010: 渐进式上下文配置（Phase 2.1 优化）
const config = this.parserConfig as ScriptParserConfig & {
  progressiveContext?: { batch1?: number; batch2to4?: number; batch5to8?: number };
};
const progressiveConfig = config.progressiveContext || {
  batch1: 2500, // 批次 1: 完整上下文（15 个分镜）
  batch2to4: 1000, // 批次 2-4: 简化上下文（12 个分镜）
  batch5to8: 500, // 批次 5+: 极简上下文（10 个分镜）
};

const shotGen = this.calculateShotGeneration(
  content,
  scenes,
  allSceneContents.length,
  episodePlanEstimate
);
console.log(
  `[ScriptParser] Target: ${shotGen.targetShots} shots (${shotGen.keyShots} key + ${shotGen.optionalShots} optional)` +
    (episodePlanEstimate ? ` [Episodes: ${episodePlanEstimate.totalEpisodes}]` : '')
);
console.log(`[ScriptParser] Phase 2.1 Progressive Context Config:`, progressiveConfig);
console.log(
  `[ScriptParser] Batch-Context Mapping: ` +
    `Batch1: 15 shots + ${progressiveConfig.batch1} chars, ` +
    `Batch2-4: 12 shots + ${progressiveConfig.batch2to4} chars, ` +
    `Batch5+: 10 shots + ${progressiveConfig.batch5to8} chars`
);
```

**在批次循环中修改上下文处理**（约 4777 行位置）:

```typescript
// ISSUE-010: 根据批次号动态选择上下文长度（Phase 2.1 优化）
let contextLength: number;
let expectedBatchSize: number;

if (currentBatch === 1) {
  contextLength = progressiveConfig.batch1;
  expectedBatchSize = 15; // 第一批：15 个分镜
} else if (currentBatch <= 4) {
  contextLength = progressiveConfig.batch2to4;
  expectedBatchSize = 12; // 中间批次：12 个分镜
} else {
  contextLength = progressiveConfig.batch5to8;
  expectedBatchSize = 10; // 后期批次：10 个分镜
}

// 动态截取上下文
const batchSceneContents = allSceneContents.substring(0, contextLength);
console.log(
  `[ScriptParser] Batch ${currentBatch}: ${actualBatchSize} shots (expected: ${expectedBatchSize}) + ${contextLength} chars context`
);
```

**验证点**:

- ✅ 第一批：15 个分镜 + 2500 字符
- ✅ 中间批次：12 个分镜 + 1000 字符
- ✅ 后期批次：10 个分镜 + 500 字符
- ✅ 日志输出清晰
- ✅ 类型检查通过

---

### 执行流程

#### Step 1: 实施优化（30-60 分钟）

1. ✅ 修改 `DynamicTimeoutCalculator.ts`
2. ✅ 修改 `scriptParser.ts`（动态批次调整）
3. ✅ 修改 `scriptParser.ts`（渐进式上下文）
4. ✅ 运行类型检查：`npm run type-check`

#### Step 2: 测试验证（等待用户通知）

1. ⏳ 用户测试优化效果
2. ⏳ 使用《暗流》剧本测试（1257 字）
3. ⏳ 验证分镜生成成功（目标：90-110 个）
4. ⏳ 验证质量评分（目标：>75/100）
5. ⏳ 检查浏览器控制台日志

#### Step 3: 提交与部署（测试通过后）

1. ✅ 创建 Git 提交：

   ```bash
   git add -A
   git commit -m "分镜生成优化：超时与批次动态调整

   Phase 1: 紧急修复
   - 任务 1.1: 分镜生成安全系数提高 (2.0→2.5-3.0)
   - 任务 1.2: 动态批次大小调整 (20→15, 动态 8-25)

   Phase 2: 中期优化
   - 任务 2.1: 渐进式上下文配置优化

   测试结果:
   - 分镜生成成功率：0% → 100% ✅
   - 分镜数量：0 → 92 个 ✅
   - 质量评分：0 → 80/100 ✅
   - 超时失败率：100% → 0% ✅"
   ```

2. ✅ 切换到 master 分支：

   ```bash
   git checkout master
   ```

3. ✅ 合并优化代码：

   ```bash
   git merge -X theirs <优化分支名>
   # 或者直接复制文件后提交
   ```

4. ✅ 推送到 GitHub：
   ```bash
   git push origin master
   ```

---

### 预期测试结果

**成功标志**：

```
[ScriptParser] Phase 2: Shot calculation with episode plan:
platform=90 (70%) + density=98 (30%) = final=92

[ScriptParser] Target: 92 shots (65 key + 27 optional)

[DynamicTimeoutCalculator] Calculated timeout for shots:
  - Average response time: 78833ms
  - Dynamic safety factor: 2.30
  - Applied timeout: 180000ms

[ScriptParser] Batch 1: 15 shots + 2500 chars context
[ScriptParser] Batch 1 complete: 15 shots, response time: 60000ms
...
[ScriptParser] Adjusting batch size: 15 → 12 (response time: 100791ms > 90000ms)
...
[ScriptParser] Final result: 92 shots (78 key + 14 optional) in 8 batches
[ScriptParser] Shot quality score: 80/100
```

---

### 关键检查点

**类型检查**:

```bash
npm run type-check
```

必须通过 ✅

**Git 状态**:

```bash
git status
git log --oneline -3
```

确认在正确的分支

**文件修改**:

- ✅ `services/parsing/DynamicTimeoutCalculator.ts`
- ✅ `services/scriptParser.ts`

---

### 注意事项

1. **不要创建独立分支**：直接在 master 分支上修改，或确保从 master 创建分支
2. **测试通过前不要提交**：等用户确认测试结果后再提交
3. **保持类型安全**：所有修改必须通过 `npm run type-check`
4. **记录测试日志**：保存浏览器控制台日志作为验证证据

---

### 联系信息

**测试完成后请提供**：

1. ✅ 分镜生成数量
2. ✅ 质量评分
3. ✅ 浏览器控制台日志文件路径
4. ✅ 是否有任何错误或警告

---

**请开始执行优化任务！测试完成后通知我，我会帮您提交到 master 分支并推送到 GitHub。** 🚀
